import Button from "react-bootstrap/Button";

function Hero() {
  return (
    <div className="hero" id="hero">
      <h1>Max Pressure</h1>
      <h3>We sweat the small stuff so you don’t have to.</h3>
      <Button variant="primary">Get an Estimate</Button>
    </div>
  );
}

export default Hero;
